#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int longestOnes(vector<int>& A, int K) {
        
        int length = 0, maxLength = 0, numZero = 0;

        int left = 0, right = 0;

        while (right < A.size())
        {
            
        }
        
    }
};