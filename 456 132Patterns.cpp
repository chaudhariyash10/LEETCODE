#include <bits/stdc++.h>

using namespace std;

class Solution
{
public:
    bool find132pattern(vector<int> &nums)
    {
        if(nums.size() < 3)
            return false;

        stack<int> st;

        
    }
};

int main()
{
    vector<int> nums{3, 1, 4, 2};
    Solution sol;
    cout << nums.size();
    cout << sol.find132pattern(nums);
}