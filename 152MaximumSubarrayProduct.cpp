#include <stdc++.h>

using namespace std;

class Solution
{
public:
    int maxProduct(vector<int> &nums)
    {
        int maxPro = INT_MAX, curPro = 1;

        for (int i = 0; i < nums.size(); i++)
        {
            for (int j = i; j < nums.size(); j++)
            { 
                
            }
        }
        return maxPro;
    }
};
